<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.erp.inventory.mapper.InventoryTransactionMapper">

    <!-- update/delete 매퍼는 두지 않는다 (append-only, docs/adr/ADR-015) -->
    <insert id="insertTransaction" parameterType="egovframework.erp.inventory.domain.InventoryTransactionVO">
        INSERT INTO inventory_transaction
            (item_id, transaction_type, quantity, purchase_request_id, sales_order_id, created_by)
        VALUES
            (#{itemId}, #{transactionType}, #{quantity}, #{purchaseRequestId}, #{salesOrderId}, #{createdBy})
    </insert>

</mapper>
